/**
 * Search-related enums
 * Shared across API Gateway and Search Microservice
 */

export enum TripType {
    ONE_WAY = 'one_way',
    ROUND_TRIP = 'round_trip',
}

export enum CabinType {
    ECONOMY = 'economy',
    BUSINESS = 'business',
    FIRST = 'first',
}
